import UnifiedRequestList from '@app/components/UnifiedRequestList';
import type { NextPage } from 'next';

const RequestsPage: NextPage = () => {
  return <UnifiedRequestList />;
};

export default RequestsPage;
